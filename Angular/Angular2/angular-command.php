<?php
Component
=> ng generate component NamaKomponen  || ng g c NamaKomponen
=> ng generate component NamaFolder/NamaKomponen

Service
=> ng generate service NamaService || ng generate s NamaService || ng g service NamaService || ng g s NamaService
=> ng generate service NamaFolder/NamaService

Pipe
=> ng generate pipe NamaPipe || ng generate p NamaPipe || ng g pipe NamaPipe || ng g p NamaPipe
=> ng generate pipe NamaFolder/NamaPipe

Directive
=> ng generate direvtive NamaDirective || ng generate d NamaDirective || 
   ng g directive NamaDirective || ng g d NamaDirective
=> ng generate direvtive NamaFolder/NamaDirective

Class
=> ng generate class NamaClass || ng generate cl NamaClass || ng g class NamaClass || ng g cl NamaClass
=> ng generate class NamaFolder/NamaClass

Interface
=> ng generate interface NamaInterface || ng generate i NamaInterface || 
   ng g interface NamaInterface || ng g i NamaInterface
=> ng generate interface NamaFolder/NamaInterface

Enum 
=> ng generate enum NamaEnum || ng generate e NamaEnum || ng g enum NamaEnum || ng g e NamaEnum
=> ng generate enum NamaFolder/NamaEnum


NB:Lihat File penamaan-folder.php
	